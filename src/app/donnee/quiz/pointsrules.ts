export class PointsRules {
  has_rule: boolean;
  point_max: number;
  point_min: number;
  rubrique_owner: string;
  rubrique_owner_id: number;
  rubrique_target: string;
  rubrique_target_id: number;
  rubrique_target_poids: number;
  active: boolean;
}
